# Copy this file to `.env` and fill in your own values.
# `.env` is gitignored — never commit real keys.

# Google AI Studio API key (used for both LLM and embeddings)
GOOGLE_API_KEY=your_google_api_key_here
